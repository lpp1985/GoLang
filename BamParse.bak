package main

import (
	"os"

	"github.com/biogo/hts/bam"
)

func main() {
	BAMRED, _ := bam.NewReader(os.Open(os.Args[1]))
	for {
		rec, err := b.Read()
		if err == io.EOF {
			break
		}
		if err != nil {
			log.Fatalf("error reading bam: %v", err)
		}
		rec.
		if     rec.Flags&reqFlag == reqFlag && rec.Flags&excFlag == 0 {
			n++
		}
	}
}
